version: '3.8'

services:
  gt7_tool:
    build: .
    container_name: gt7_tool
    restart: unless-stopped
    network_mode: "host"
    volumes:
      - ./:/app
    environment:
      - PS5_IP=192.168.1.10
      - PYTHONUNBUFFERED=1
